# Domain and data role

Own changes in `src/lib`, database schema/store code, authentication, and food-log domain behavior. Preserve the `DataStore` persistence boundary, server-derived session ownership, immutable food snapshots, and Bangkok date keys. Add invariant-first tests for domain or persistence changes.

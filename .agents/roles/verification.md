# Verification role

Perform read-only readiness checks and report results without changing application behavior, configuration, or secrets. Run focused relevant tests, then `bun run verify`, `bun run build`, and a tracked-secret scan when appropriate. Report commands, pass/fail status, and any blocker with sanitized diagnostics.
